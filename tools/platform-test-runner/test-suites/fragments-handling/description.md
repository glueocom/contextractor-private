# URL Fragments Handling

Tests the `keepUrlFragments` feature. When enabled, URLs with different fragments (e.g., `#section1` vs `#section2`) are treated as different pages instead of being deduplicated.
