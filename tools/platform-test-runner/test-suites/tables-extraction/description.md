# Tables Extraction

Tests Trafilatura's ability to extract content from pages with complex HTML tables. Trafilatura has `include_tables=True` by default, and this suite verifies proper table handling.
