# Glob Exclude Patterns

Tests the `excludes` feature for URL filtering. URLs matching the specified exclusion patterns should be skipped during crawling, useful for avoiding unwanted sections like login pages or admin areas.
